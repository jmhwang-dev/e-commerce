# CHANGELOG
- "Match the hostname with the service name

# RUN

```bash
python3 -m pip install --upgrade pip wheel setuptools
python3 -m pip install --upgrade acryl-datahub

datahub docker quickstart --quickstart-compose-file infra/datahub/docker-compose.yml
```